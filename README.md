### PyTest for Data Engineering: A practical guide with PySpark

1. Environment setup
- Install required packages in requirements.txt
- Install Java (once installed `JAVA_HOME` will be set)

2. Execute data unit tests: `python3 main.py`
